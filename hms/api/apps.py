from django.apps import AppConfig


class ApiConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'api'

    def ready(self):
        import api.emails
        import api.notifications
        import api.tasks
        import api.scheduled_tasks